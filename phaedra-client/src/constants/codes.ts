const ERROR_CODE: number = -1;
const DEFAULT_IMAGE_ID: number = 1;
const LANGUAGE: string = 'en-US';
const TIMEZONE: string = 'America/Vancouver';

export { ERROR_CODE, DEFAULT_IMAGE_ID, LANGUAGE, TIMEZONE };
